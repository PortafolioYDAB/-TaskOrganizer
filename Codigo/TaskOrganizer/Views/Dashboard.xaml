<Window x:Class="TaskOrganizer.Views.Dashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskOrganizer.Views"
        Height="426" Width="772">

    <Grid HorizontalAlignment="Right" Width="772">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="121*"/>
            <ColumnDefinition Width="651*"/>
        </Grid.ColumnDefinitions>
        <Grid Margin="5,0,10,0" Grid.Column="1"/>

        <!-- PANEL IZQUIERDO (columna 0) -->
        <Border  Grid.Column="0" Style="{StaticResource styleBorder}" Margin="0,5,5,5">
            <Grid  Style="{StaticResource gridBackGround}" Margin="8,8,8,8">
                <!-- Aquí va el contenido principal -->
                <StackPanel >

                </StackPanel>
            </Grid>
        </Border>



        <!-- CONTENIDO PRINCIPAL (columna 1) -->
        <Grid Margin="111,0,10,0" Grid.ColumnSpan="2">
            <!-- Aquí va el contenido principal -->
        </Grid>

    </Grid>
</Window>
